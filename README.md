QX: Minimal vendor-neutral quantum SDK (local simulator only).

Configuration
-------------

Runtime configuration is documented in `docs/config.md` and can be adjusted
programmatically using the `qx.config` API (for example `set_max_qubits()`).

See `docs/getting_started.md` for quick examples and `docs/config.md` for full
API details.
